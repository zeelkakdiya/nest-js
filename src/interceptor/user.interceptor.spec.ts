import { InterceptorInterceptor } from './user.interceptor';

describe('InterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptorInterceptor()).toBeDefined();
  });
});
